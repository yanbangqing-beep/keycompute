[package]
name = "keycompute-ratelimit"
version = "0.1.0"
edition = "2024"

[dependencies]
keycompute-types = { workspace = true }
async-trait = "0.1"
dashmap = "6"
tokio = { version = "1", features = ["sync", "time", "rt", "macros"] }
tracing = "0.1"
uuid = { version = "1", features = ["v4"] }
